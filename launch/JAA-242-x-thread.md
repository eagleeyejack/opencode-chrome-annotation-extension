# JAA-242 - X launch thread

Post the MP4 (not GIF) with tweet 1. US + EU overlap window (roughly 1-5pm CET).

## Tweet 1

> Plannotator reviews what your agent wrote. This annotates what it built.
>
> I click the broken element in my live app. My agent gets the screenshot, the exact selector, and my instruction. It fixes it.
>
> [video]

## Tweet 2

> How it works: a side panel connects the tab to an OpenCode session. Click an element, type what should change, queue more, hit Send all.
>
> The agent receives cropped screenshots + real DOM metadata (selector, tag, role, ARIA) - not vibes.

## Tweet 3

> Everything stays on 127.0.0.1. No accounts, no telemetry, no third-party servers.
>
> Also: close stale agent sessions from the panel. Two clicks. Gone.

## Tweet 4

> It's open source (GPL-3.0) - an honest fork of JodusNodus's opencode-chrome-annotation, rebuilt around a side panel. Plugin install is one config line, extension loads unpacked today (store listing in review).
>
> Repo: <link>

## Alt standalone post (if a thread feels heavy)

> I got tired of describing UI bugs to my agent in paragraphs. Now I click the element, type the fix, send. Screenshot + selector + instruction, straight into OpenCode. [video] Repo in reply.
